import { ImageUrlVDirective } from './image-url-v.directive';

describe('ImageUrlVDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageUrlVDirective();
    expect(directive).toBeTruthy();
  });
});
